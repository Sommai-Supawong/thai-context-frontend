# V3 interrupted-session recovery — 2026-09-13

Continued the existing uncommitted V3 implementation after reading both design documents, inspecting the working tree and diff, and running the current app in Chrome through Playwright. Existing V3 components, dependency changes, and prior document deletions were preserved.

## Changes in this recovery session

- `src/app/globals.css`: fixed composer centering independently of its animated transform, retained reversible entrance/exit, added restrained hover feedback and desktop share positioning.
- `src/components/search/PersistentSearchComposer.tsx`: defer inert/hidden interaction state until the fade completes; preserve the mounted composer and typed query across reversals.
- `src/components/layout/MorphingNavbar.tsx`: search shortcut returns to the usable Hero input when the footer has hidden the composer.
- `src/components/hero/HeroSection.tsx`: check WebGL2 support before mounting the asynchronous renderer, retaining the poster and usable search fallback.
- `src/components/hero/Hero3DScene.tsx`: reset pointer displacement when viewport/pointer capabilities change; constrain book size to keep the search area clear.
- `src/components/search/SearchResults.tsx`: stop pronunciation when filtering changes the selected word.
- `src/lib/audio-manager.ts`: prevent delayed resume completion from reviving stale playback state after a new search or stop.
- `src/components/pronunciation/PronunciationButton.tsx` and `src/components/ui/Icon.tsx`: distinguish paused playback with a resume label and play icon.
- `src/components/share/ShareResultButton.tsx`: anchor the compact desktop share dialog near its trigger; render copy feedback outside transformed result ancestors.
- `src/lib/search-types.ts`: validate source arrays even when legacy evidence is supplied alongside them.
- `src/app/icon.svg`: add the existing book brand mark as the application icon, resolving the browser favicon 404.
- `tests/browser/v3.spec.ts` and `tests/browser/v3-polish.spec.ts`: add composer bounds, WebGL fallback, motion reversal, audio, clipboard and mobile sheet coverage; retain screenshots.
- `tests/search-flow.test.tsx`, `tests/state-api.test.ts`, and `tests/audio-manager.test.ts`: support the explicit GPU stub and cover source validation and stale audio resume.
- `next-env.d.ts`: regenerated by Next.js during the production build.

## Preserved and verified

The existing central Hero cinematic timeline remains in place. Hero search starts its request alongside the cinematic; persistent search updates results without replaying it. The V3 workspace, candidate selection, comparison controls, evolution keyboard navigation, dialect selection, evidence drawer and shared-word deep links remain available.

## Verification

- Production build: passed.
- Standalone typecheck: passed.
- ESLint: passed with zero warnings.
- Unit/integration tests: 24 passed.
- Chrome browser suite: 12 passed on development and 12 passed on the production build.
- Responsive browser widths: 375, 430, 768, 900, 1024, 1280 and 1440 pixels.
- Browser checks include actual Hero search, cinematic, repeated search, selection, share/deep-link restore, evidence keyboard flow, footer shortcut, rapid scroll reversals, composer exit/entrance, ambient scene changes and reduced-motion stability.
- Pronunciation checks use a local PCM WAV fixture through real Chrome audio playback: play, pause, resume, stop on selection/search, and invalid audio feedback.
- Clipboard copy and the mobile share sheet were exercised in Chrome.
- Production HTTP checks: passed for 0/1/2/3/5 results, delayed upstream, malformed upstream, upstream failure fallback, invalid input and static assets.
- No page or console errors in the successful main end-to-end flow; no hydration errors observed. The initial composer failure, WebGL initialization failure path and missing favicon were addressed.
- `git diff --check`: passed.
- Visually reviewed desktop Hero/workspace and mobile/tablet screenshots in `test-results/`.

## Running app and limits

The original development server remains at http://localhost:3000. The verified production preview is running at http://localhost:3002.

Browser tests used Chrome with emulated viewport sizes, not physical phones or Safari. Native operating-system share completion and installed Thai TTS voice quality were not verified; recorded-audio behavior, copy fallback and deep links were verified. External production backend content was not validated; HTTP tests used a controlled upstream. The installed Three/R3F stack emits a non-failing `THREE.Clock` deprecation warning in development.
